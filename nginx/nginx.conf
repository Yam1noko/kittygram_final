server {
    listen 80;
    server_name yavits.com www.yavits.com;

    client_max_body_size 10m;

    location /api/ {
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_pass http://kitty_backend:9000/api/;
    }

    location /admin/ {
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_pass http://kitty_backend:9000/admin/;
    }

    location /media/ {
        alias /kitty_media/;
    }

    location / {
        alias /kitty_static/;
        try_files $uri $uri/ /index.html;
    }
}

server {
    listen 80;
    server_name дцз.рф www.дцз.рф xn--d1aqf.xn--p1ai www.xn--d1aqf.xn--p1ai;

    client_max_body_size 10m;

    location /api/ {
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_pass http://taski_backend:8000/api/;
    }

    location /admin/ {
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_pass http://taski_backend:8000/admin/;
    }

    location / {
        alias /taski_static/;
        try_files $uri $uri/ /index.html;
    }
}